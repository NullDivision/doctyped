// @flow

<% refs.forEach(function(ref) { %>import <%= ref %> from './<%= ref %>';
<% }); -%>
<% if (Object.keys(additionalTypes).length) { %>

<% Object.keys(additionalTypes).forEach(function(value, key) { -%>
export type <%= key %> = <%- value %>;
<% }); -%>
<% } -%>

export type <%= modelName %> = {
<% Object.keys(modelSchema).forEach(function(key) { -%>
  <%= key %>: <% if (!modelSchema[key].required) { -%>?<% } -%><%- modelSchema[key].type %>;
<% }); -%>
};
