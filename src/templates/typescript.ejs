<% if (importTypes.length) {importTypes.forEach((name) => { %>import { <%= name %> } from './<%= name %>.ts';
<% }) %>
<% } _%>
<% if (exportTypes.length) {
    exportTypes.forEach((exportType) => { %>
export enum <%= exportType.name %> {<% exportType.type.split('|').forEach((item) => {%>
    <%= item.replace(/\'/g, '') %>;<%}) %>
}
<% })
} _%>
export interface <%= name %> {
<%_ Object.entries(properties).forEach(([propName, { required, type }]) => { -%>
  <%= propName %><% if (!required) { %>?<% } %>: <%- type %>;
<% }); -%>
};